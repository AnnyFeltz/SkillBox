[phases.setup]
nixPkgs = ['php81', 'composer', 'mysql', 'nodejs_18']

[phases.install]
cmds = [
  'composer install --no-interaction --prefer-dist --optimize-autoloader',
  'npm ci',
  'php artisan key:generate',
  'php artisan migrate --force'
]

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'php artisan serve --host=0.0.0.0 --port=$PORT'
